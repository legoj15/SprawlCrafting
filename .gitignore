# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea/*
!.idea/scopes

# gradle
build
.gradle

# other
eclipse
run
runs
run-*
logs

# Machine-specific test harness (Server1/WinRM hostnames, local Prism/JDK paths, staged jars) —
# same convention as BuildCraft. Third-party clones run `test` + `runGameTestServer` instead.
/testing/

# Release tooling is versioned (release/Invoke-Release.ps1, the module, README, and release.json with
# placeholder store IDs) — only the live publishing credentials are excluded.
/release/config/secrets.ps1

# Claude Code session state
.claude/
